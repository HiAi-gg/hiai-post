// Re-export from renderer.ts — nodes.ts kept for backwards compatibility
export {
	renderElement,
	renderBackground,
	preloadImages,
	createElementNode,
	loadImage,
	getCachedImage,
	updateNode,
} from "./renderer";
