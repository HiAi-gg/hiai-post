services:
  hiai-post-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hiai-post-api
    ports:
      - "50300:3000"
    env_file: .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-hipost}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/hiai_post
      REDIS_URL: redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - hiai-post

  hiai-post-frontend:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: hiai-post-frontend
    ports:
      - "50301:3000"
    env_file: .env
    environment:
      ORIGIN: ${FRONTEND_URL:-http://localhost:50301}
      API_URL: http://hiai-post-api:3000
    depends_on:
      hiai-post-api:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - hiai-post

  postgres:
    image: pgvector/pgvector:pg18
    container_name: hiai-post-pg
    environment:
      POSTGRES_DB: hiai_post
      POSTGRES_USER: ${POSTGRES_USER:-hipost}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5436:5432"
    volumes:
      - hiai-post-pg:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hipost} -d hiai_post"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - hiai-post

  redis:
    image: redis:8.6-alpine
    container_name: hiai-post-redis
    ports:
      - "6383:6379"
    volumes:
      - hiai-post-redis:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - hiai-post

volumes:
  hiai-post-pg:
  hiai-post-redis:

networks:
  hiai-post:
    driver: bridge
